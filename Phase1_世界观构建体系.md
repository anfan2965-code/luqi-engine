# Phase 1: 世界观构建体系

> **版本**: v1.0.0  
> **更新日期**: 2026-05-05  
> **状态**: 已实现

## 概述

鹿栖引擎的世界观构建体系是一个**结构化要素提取与渲染引导系统**，负责从用户输入的世界观设定中提取关键要素、分类到标准维度、建立要素间关联、检测逻辑矛盾，最终生成给LLM的结构化渲染引导文本。

**核心设计理念**：用户提供的原始世界观（文本/JSON/CSV）不是直接塞给LLM，而是经过"提取→分类→关联→冲突检测→渲染"五步流水线处理，确保LLM获得的是**结构化、一致性强、无矛盾**的世界观信息。

## 架构

```
用户输入（text/json/csv）
    │
    ▼
┌──────────────────┐
│ extract_elements() │  要素提取 — 正则匹配 + JSON解析 + CSV解析 + 句子分割
└──────┬───────────┘
       │ Dict[str, Any] {elements, source_type, total}
       ▼
┌──────────────────┐
│ classify_elements() │  要素分类 — 9维关键词匹配
└──────┬───────────┘
       │ Dict[str, List[Dict]] {geography, society, culture, ...}
       ▼
┌──────────────────┐
│ build_relations()   │  关联构建 — Jaccard相似度邻接图
└──────┬───────────┘
       │ Dict[str, Any] {adjacency, node_count, edge_count}
       ▼
┌──────────────────┐
│ detect_conflicts() │  冲突检测 — 否定词+实体名模式匹配
└──────┬───────────┘
       │ List[ConflictReport]
       ▼
┌──────────────────┐
│ render_guidance()  │  渲染生成 — Markdown格式结构化文本
└──────┬───────────┘
       │ str (Markdown)
       ▼
   LLM System Prompt
```

## 一、要素提取 (extract_elements)

支持三种输入格式的自动识别和提取：

| 格式 | 提取方式 | 输出结构 |
|------|----------|----------|
| **Text** | 正则匹配列表项(`-`/`*`/`•`) + 标题(`#`) + 句子分割(≥3字) | `[{name, content}]` |
| **JSON** | 递归遍历 list/dict，取 name/title + description/content | `[{name, content}]` |
| **CSV** | 首行表头映射，逐行读取 | `[{name, content}]` |

**文本提取的优先级**：
1. 列表项正则 `[-•*]\s*(.+?)(?:[:：]\s*(.+?))?(?:\n|$)` — 匹配 `- 名称: 内容`
2. 标题正则 `#{1,3}\s+(.+?)(?:\n|$)` — 匹配 `### 章节名`
3. 句子分割 — 以句号/感叹号/问号切分，保留 ≥3 字的句子作为要素

**边界防御**：空输入返回空列表；JSON 解析失败返回零元素结果。

## 二、9维分类系统 (classify_elements)

每个提取出的要素被分配到以下 9 个标准维度之一：

| 维度 | 中文 | 示例关键词 |
|------|------|-----------|
| geography | 地理 | 山脉、河流、海洋、城市、地图、气候 |
| society | 社会 | 阶级、组织、公会、家族、部落 |
| culture | 文化 | 语言、艺术、节日、习俗、传统 |
| history | 历史 | 战争、纪元、朝代、事件、传说 |
| magic_system | 法术 | 魔法、法术、咒语、魔力、元素、符文 |
| technology | 科技 | 机械、工程、发明、武器、工具 |
| politics | 政治 | 王国、帝国、联盟、条约、权力 |
| religion | 宗教 | 神、神殿、祭祀、信仰、教派 |
| ecology | 生态 | 生物、怪物、植物、动物、物种 |

无法归类的要素放入 `unclassified` 分类桶。

**分类算法**：对每个要素的 `name + content` 拼接文本，在 9 个维度的关键词库中做关键词命中计数。选择命中数最多的维度。若所有维度命中数为 0，则归类为 unclassified。

## 三、关联关系构建 (build_relations)

要素间的关系通过 **Jaccard 相似度** 计算：

```
Jaccard(A, B) = |words_A ∩ words_B| / |words_A ∪ words_B|
```

相似度 → 权重映射：

| Jaccard 范围 | 关系权重 | 含义 |
|-------------|---------|------|
| > 0.3 | 0.8 (STRONG) | 强关联 — 共享大量关键词 |
| > 0.1 | 0.5 (MEDIUM) | 中等关联 — 有部分重叠 |
| > 0.05 | 0.2 (WEAK) | 弱关联 — 少量重叠 |
| ≤ 0.05 | 无关系 | 不建立边 |

输出为**邻接表**形式的图结构，包含节点数和边数统计。

**性能参数**：
- 每个维度的渲染上限：10 条要素
- 每个源节点的最大展示：20 个
- 每个目标节点的最大展示：3 个

## 四、冲突检测 (detect_conflicts)

当前实现 3 类冲突检测模式：

| 类型 | 中文名 | 检测方法 |
|------|--------|----------|
| temporal | 时间线矛盾 | （预留接口） |
| causal | 因果逻辑矛盾 | （预留接口） |
| attribute | 属性冲突 | 否定词 + 实体名匹配 |

**属性冲突检测**：扫描每对要素的内容描述，如果发现否定词（不/非/无/禁止/不可能/not/no/impossible）同时出现另一个要素的名称，则判定存在属性冲突。

冲突报告包含：
- 冲突 ID（EntityId）
- 冲突类型
- 描述文字
- 严重程度（默认 0.8）
- 涉及实体列表
- 建议解决方案（保留/修改/折中）

## 五、渲染输出 (render_guidance)

最终输出为 Markdown 格式的结构化文本，包含两个主要章节：

```markdown
# 世界观设定

## geography
- **山脉**: 连绵起伏的天脊山脉横贯大陆北部...
- **河流**: 银月河从雪山发源...

## society
- **公会**: 铁匠公会是最大的工匠组织...

## ...

## 要素关联
- 山脉 ↔ 银月河 (权重: 0.8)
- 铁匠公会 ↔ 城市 (权重: 0.5)
```

这个 Markdown 文本会被注入 LLM 的 system prompt 中，作为世界观约束条件。

## 快照与序列化

WorldViewRenderer 支持 ISnapshotable 接口：
- `save_snapshot()` → 序列化 elements/relations/world_model 为 Dict
- `load_snapshot(data)` → 从 Dict 恢复完整状态

这允许引擎在快照恢复时保持世界观的完整性。

## 设计权衡

| 决策 | 原因 | 替代方案 |
|------|------|----------|
| 9维固定分类而非NLP语义分析 | 零外部依赖、确定性可复现 | 使用BERT做语义分类（需额外依赖） |
| Jaccard关键词相似度而非向量嵌入 | 轻量、快速、无需预训练模型 | Sentence-BERT语义向量（更准但更重） |
| 否定词模式匹配而非NLI推理 | 可控、可解释 | NLI模型做矛盾检测（黑盒） |
| 邻接表而非复杂图数据库 | 引擎内嵌场景下足够 | Neo4j/NetworkX（过度工程） |
